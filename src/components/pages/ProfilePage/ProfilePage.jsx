import {UserCard} from '../../UserCard';
import { UserForm } from '../../ui/UserForm';

const ProfilePage = () => {
    return (
        <div>
            <h2>ProfilePage</h2>
            <UserCard />
            <UserForm/>
        </div>
        
    );
};

export default ProfilePage;
